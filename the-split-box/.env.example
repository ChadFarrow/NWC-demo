# The Split Box Configuration

# Your AlbyHub NWC connection string
# Get this from your AlbyHub: Settings > Connections > Create New Connection
# Make sure to enable: make_invoice, lookup_invoice, pay_keysend, get_balance
NWC_CONNECTION_STRING=nostr+walletconnect://your_wallet_pubkey?relay=wss://relay.getalby.com/v1&secret=your_secret_key

# Server port (optional, defaults to 3333)
PORT=3333

# CORS origin (optional, defaults to all origins)
# Set this to your NWC Demo URL for production
CORS_ORIGIN=http://localhost:3003